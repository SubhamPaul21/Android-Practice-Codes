<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/wordsListRelativeLayout"
    tools:background="@color/blue_izis"
    android:layout_width="match_parent"
    android:layout_height="@dimen/image_dim"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/wordImageView"
        android:layout_width="@dimen/image_dim"
        android:layout_height="@dimen/image_dim"
        tools:srcCompat="@tools:sample/avatars[1]" />

    <TextView
        android:id="@+id/miwokTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginStart="15dp"
        android:layout_marginTop="22dp"
        android:layout_toEndOf="@id/wordImageView"
        android:textColor="#FFF"
        android:textSize="16sp"
        android:textStyle="bold"
        tools:text="lutti" />

    <TextView
        android:id="@+id/englishTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/miwokTextView"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="15dp"
        android:layout_marginBottom="22dp"
        android:layout_toEndOf="@id/wordImageView"
        android:textColor="#FFF"
        android:textSize="16sp"
        tools:text="three" />

    <ImageView
        android:id="@+id/playBtnImageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerInParent="true"
        android:layout_marginEnd="15dp"
        app:srcCompat="@style/Widget.AppCompat.DrawerArrowToggle" />
</RelativeLayout>